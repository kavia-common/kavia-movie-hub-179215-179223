# Flask backend environment variables
# Copy this file to .env and set the values appropriately. Do NOT commit .env to version control.

# REQUIRED: Supabase configuration (server-side only, never expose service key to frontend)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Optional: Flask host/port overrides
FLASK_HOST=0.0.0.0
FLASK_PORT=3001

# Optional: Allowed frontend origin (CORS). You can add more origins in app/__init__.py
# FRONTEND_URL=https://your-frontend.example.com
