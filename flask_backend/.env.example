# Flask Backend Environment Variables
# Copy this file to `.env` and set the values. Do NOT commit real secrets.

# Supabase configuration (server-side only; never expose to frontend)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
